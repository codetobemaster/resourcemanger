<% provide(:title, t('users.edit.title')) %>
<div class="row">
    <%= form_for(@user) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <%= render 'fields', f: f%>
 <div class="span9 offset2">
 <div class="row">
 <div class="span3"> <%= f.submit "Save changes", class: "btn btn-large btn-primary" %> </div>
 </div></div>

    <% end %>
</div>
