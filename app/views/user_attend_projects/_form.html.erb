<%= form_for(@user_attend_project) do |f| %>
  <% if @user_attend_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_attend_project.errors.count, "error") %> prohibited this user_attend_project from being saved:</h2>

      <ul>
      <% @user_attend_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<table id="customers" >
	<tr class="alt">
	<td> <%= f.label :user %><%= f.collection_select(:user_id, User.all, :id, :name ) %>
	<%= f.label :project %><%= f.collection_select(:project_id, Project.all, :id, :name ) %>

	 <%= f.label :description %><%= f.text_area :description, :size => "20x4"%></td>
	</tr>

</table>
  <div class="actions">
    <%= f.submit "Assign To Project " , class: 'background' %>
  </div>
<% end %>
